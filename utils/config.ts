// @ts-nocheck
import yaml from 'js-yaml'
import { readdirSync as readdir, readFileSync as readfile } from "fs";
import p from "path";


let config = {  CULTUM_URL: process.env.CULTUM_URL }


export default config
